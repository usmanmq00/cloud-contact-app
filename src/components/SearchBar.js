import React from 'react'
import '../css/SearchBar.css'

const SearchBar = () => {
  return (
      <input className="searchbar" type="search" placeholder="Filter Contacts..." />
  )
}

export default SearchBar
